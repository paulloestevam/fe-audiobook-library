const HOST = import.meta.env.VITE_BACKEND_HOST || 'http://localhost:8080';

export const API_URL = `${HOST}/audiobook-library`;
export const IMAGES_URL = `${HOST}/images`;
export const DOWNLOADS_URL = `${HOST}/downloads`;
